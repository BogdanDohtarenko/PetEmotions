package com.ideasapp.petemotions.data.db.mappers

import com.ideasapp.petemotions.data.db.dbModels.DayItemInfoDbModel
import com.ideasapp.petemotions.domain.entity.calendar.DayItemInfo
import com.ideasapp.petemotions.domain.entity.calendar.Pet

/*
object PetMapper {
    fun dbModelToEntity(dbModel: PetDbModel): Pet {
        return Pet(id = dbModel.id, name = dbModel.name)
    }
    fun entityToDbModel(entity: Pet): PetDbModel {
        return PetDbModel(id = entity.id, name = entity.name)
    }
}*/
